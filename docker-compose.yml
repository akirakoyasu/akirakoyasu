version: '3'
services:
  jekyll:
    build: .
    image: akirakoyasu/jekyll
    command:
      - jekyll
      - serve
      - --force_polling
    ports:
      - 4000:4000
    volumes:
      - $PWD:/srv/jekyll
